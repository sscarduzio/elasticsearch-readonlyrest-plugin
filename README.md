# ROR benchmark history

Append-only performance KPI ledger. One reduced JMH run-record per nightly run under results/YYYY/MM/DD/; dashboard at index.html (GitHub Pages). Written ONLY by the nightly perf pipeline (ci/azure-nightly-perf.yml). See benchmarks/README.md on develop.
