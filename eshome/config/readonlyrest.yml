readonlyrest:

  access_control_rules:
    - name: "CONTAINER ADMIN - file"
      verbosity: "error"
      type: "allow"
      auth_key: "admin:container"

    - name: "es-pub7 snapshot_ro"
      x_forwarded_for:
        - es-pub7
      type: allow

    - name: "dev1"
      indices: ["dev1*"]
      type: allow
      auth_key: dev1:test