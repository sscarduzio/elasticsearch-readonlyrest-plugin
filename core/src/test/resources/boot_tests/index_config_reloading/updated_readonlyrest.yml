readonlyrest:

  access_control_rules:

    # ES container initializer need this rule to configure ES instance after startup
    - name: "CONTAINER ADMIN - changed"
      verbosity: error
      type: allow
      auth_key: admin:container