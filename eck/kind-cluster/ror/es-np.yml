apiVersion: v1
kind: Service
metadata:
  name: es-np
spec:
  type: NodePort
  ports:
  - port: 9200
    targetPort: 9200
    nodePort: 30010
  selector:
    common.k8s.elastic.co/type: elasticsearch
    elasticsearch.k8s.elastic.co/cluster-name: quickstart
