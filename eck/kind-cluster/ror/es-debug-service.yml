apiVersion: v1
kind: Service
metadata:
  name: es-debug-service
spec:
  selector:
    common.k8s.elastic.co/type: elasticsearch
    elasticsearch.k8s.elastic.co/cluster-name: quickstart
  ports:
    - protocol: TCP
      port: 8888
      targetPort: 8888