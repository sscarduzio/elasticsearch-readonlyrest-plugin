readonlyrest:
  ssl:
    enable: true
    keystore_file:  "ror-keystore.jks"
    keystore_pass: readonlyrest
    key_pass: readonlyrest

  enable: true

  access_control_rules:

  - name: "CONTAINER ADMIN"
    auth_key: admin:container

  - name: Getter
    auth_key: dev1:test
    indices: ["intentp1_*", ".kibana", ".kibana_intentp1"]
