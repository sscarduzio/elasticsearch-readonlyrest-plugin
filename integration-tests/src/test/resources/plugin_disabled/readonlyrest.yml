readonlyrest:
  enable: false

  audit:
    enabled: true
    outputs:
      - type: index

  access_control_rules:

    - name: "CONTAINER ADMIN - file"
      verbosity: "error"
      type: "allow"
      auth_key: "admin:container"
