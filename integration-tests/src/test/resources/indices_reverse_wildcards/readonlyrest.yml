readonlyrest:

  access_control_rules:

    - name: "CONTAINER ADMIN"
      verbosity: error
      type: allow
      auth_key: admin:container

    - name: "::GETTER::"
      indices: logstash-a*
      auth_key: "dev1:test"