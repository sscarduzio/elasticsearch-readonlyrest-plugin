readonlyrest:
  ssl:
    enable: true
    keystore_file: "keystore.jks"
    keystore_pass: readonlyrest
    key_pass: readonlyrest

  access_control_rules:
    - name: "Require HTTP Basic Auth (kibana dfault)"
      verbosity: error
      type: allow
      auth_key: kibana:changeme

    - name: "CONTAINER ADMIN"
      verbosity: error
      type: allow
      auth_key: admin:container

    - name: Nested field test1
      auth_key: "user1:pass"
      indices: ["*"]
      fields: ["_source", "items.endDate", "secrets"]

    - name: Nested field test2
      auth_key: "user2:pass"
      indices: ["*"]
      fields: ["_source", "items.*Date", "secrets.*"]

    - name: Nested field test3
      auth_key: "user3:pass"
      indices: ["*"]
      fields: ["~items.endDate", "~secrets"]

    - name: Nested field test4
      auth_key: "user4:pass"
      indices: ["*"]
      fields: ["~items.*Date", "~secrets.*"]