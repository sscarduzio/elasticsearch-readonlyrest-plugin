readonlyrest:
  ssl:
    enable: true
    keystoress_file: "keystore.jks"
    keystorse_pass: readonlyrest
    key_pass: readonlyrest

  access_control_rules:
    - name: "Test Rule"
      type: allow
      indices: ["*"]